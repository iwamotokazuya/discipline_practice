<div id='container'>
  <% if logged_in? %>
    <h1 class='pt-2 resultName'><%= @result.user.name %>さんの結果は・・・</h1>
  <% else %>
    <h1 class='pt-2 resultName'>あなたの結果は・・・</h1>
  <% end %>

  <% if [@result.anger, @result.sorrow, @result.joy, @result.energy, @result.calm].sum.zero? %>
    <p>エラーが発生しています。</p>
  <% else %>
    <p class='text-center pt-5 score'><span class='title'><%= @result.score %></span>点</p>
  <% end %>

  <p class='d-flex justify-content-end comment'><%= @comment %></p>
</div>

<div class='button'>
  <% if @result.record_voice.attached? %>
    <%= audio_tag(url_for(@result.record_voice), controls: true, preload: 'metadata', class: 'audio mx-auto mt-2') %>
  <% else %>
    <p>申し訳ございません。録音に失敗しました。</p>
    <%= link_to 'もう一度やりなおす', new_record_path(part: 'all'), class: 'btn btn-dark btn-rounded mx-auto mt-2' %>
  <% end %>

  <%= link_to 'https://twitter.com/share?url=#{request.url}&text=しつけ度『#{@result.score}点』だったよ！%0a%0aあなたもペットに理想的なしつけを身につけませんか？', target: '_blank', class: 'btn btn-info btn-rounded mx-auto mt-2' do %>
    <i class='fab fa-twitter'></i>結果をshareしよう！
  <% end %>

  <% if logged_in? %>
    <%= link_to 'ホームへ戻る', root_path, class: 'btn btn-warning btn-rounded mx-auto mt-2' %>
    <%= link_to 'もう一度挑戦する', new_record_path(part: 'all'), class: 'btn btn-secondary btn-rounded mx-auto mt-2' %>
    <% if @result.score >= 20 %>
      <%= link_to 'NEXT STEP', login_new_records_path, class: 'btn btn-success btn-rounded mx-auto mt-2' %>
    <% end %>
  <% else %>
    <%= link_to 'ホームへ戻る', root_path, class: 'btn btn-warning btn-rounded mx-auto mt-2' %>
  <% end %>
</div>
